# Production Environment Configuration
# Used when deployed (e.g., Fly.io, Vercel, Netlify)

# API Base URL for production
# When frontend and backend are on the same domain, use relative URL
# This automatically uses the deployed domain
VITE_API_BASE_URL=/api

# Server port (Fly.io, Render, Railway will override this)
# Default to 8080, but allow override via PORT env var
PORT=8080

# Node environment
NODE_ENV=production

# Optional: API timeout (ms)
VITE_API_TIMEOUT=30000

# Deployment-specific settings (update as needed)
# For Fly.io, these are usually set via fly.toml or secrets
# REACT_APP_ENVIRONMENT=production
