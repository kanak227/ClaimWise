# Example environment configuration for development
# Copy to .env.development (for `pnpm dev`) or .env (for general use)

# FastAPI backend URL used by the frontend during development
# Use absolute URL to ensure the dev proxy targets a valid host
VITE_API_BASE_URL=http://localhost:8000

# WebSocket base (optional). If omitted, it's derived from VITE_API_BASE_URL
# VITE_WS_BASE_URL=ws://localhost:8000

# Request timeout (ms)
VITE_API_TIMEOUT=30000
