# Development Environment Configuration
# Used when running: pnpm dev

# API Base URL for development
# During development, Vite dev server runs on :8080 with Express middleware
# API calls will be proxied to the Express server running on the same port
VITE_API_BASE_URL=/api

# Server port (used in production server, dev server ignores this)
PORT=8080

# Node environment
NODE_ENV=development

# Optional: API timeout (ms)
VITE_API_TIMEOUT=30000
